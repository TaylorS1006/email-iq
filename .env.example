HUBSPOT_ACCESS_TOKEN=your-private-app-token-here

# Optional — only needed once the Cloudflare Worker in cloudflare-worker/
# is deployed. Set this to bake Co-write into the published index.html,
# pointed at that Worker. See cloudflare-worker/README.md.
COWRITE_WORKER_URL=
